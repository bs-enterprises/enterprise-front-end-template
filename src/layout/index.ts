/**
 * Layout Components Export
 * 
 * This folder contains application-specific layout implementations.
 * These are NOT reusable components - they are configured for this specific application.
 * 
 * For reusable shell components, see: @/components/AppShell
 */

export { LayoutWithAppShell } from './LayoutWithAppShell';
